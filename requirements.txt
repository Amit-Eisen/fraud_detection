# Fraud Detection Project - Python Dependencies
# Credit Card Fraud Detection using XGBoost
# 
# Installation:
#   python3 -m venv .venv
#   source .venv/bin/activate  (Linux/Mac)
#   .venv\Scripts\activate     (Windows)
#   pip install -r requirements.txt

# Core data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0          # For efficient Parquet file handling

# Date/time handling
python-dateutil>=2.8.0
pytz>=2023.0

# Machine Learning
scikit-learn>=1.3.0      # For train/test split, metrics, preprocessing
xgboost>=2.0.0           # Main model - gradient boosting
lightgbm>=4.0.0          # Alternative gradient boosting (baseline)

# Hyperparameter Optimization
optuna>=3.3.0            # Bayesian optimization for hyperparameter tuning

# Visualization
matplotlib>=3.7.0        # Core plotting library
seaborn>=0.12.0          # Statistical visualizations (optional, but used in some scripts)

# Model Persistence
joblib>=1.3.0            # For saving/loading models (included with scikit-learn)

# Progress bars (optional but helpful)
tqdm>=4.65.0             # Progress bars for long-running operations
